{
  "name": "blogs",
  "version": "0.0.1",
  "description": "blogs application ",
  "main": "start",
  "scripts": {
    "start": "cross-env NODE_ENV=production node app.js",
    "development": "cross-env NODE_ENV=test nodemon app.js",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand --forceExit"
  },
  "author": "Helge Kim",
  "license": "ISC",
  "devDependencies": {
    "jest": "^28.1.0",
    "nodemon": "^2.0.16",
    "supertest": "^6.2.3"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "crossenv": "^0.0.2-security",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.3.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
